# Taylor級数・剰余・収束半径：教科書

Course 01｜微積分

## このTopicで解く問題

有限次数のTaylor近似を無限級数へ伸ばすと、いつ元の関数そのものになるのか。

## なぜこの概念が必要か

Taylor多項式を高次数へしただけでは自動的に関数へ一致しない。差 R_n(x) が0へ行くことを別に確認する必要がある。

## 図の各要素は何を表しているか

<img src="/visuals/course-01/calc-taylor-series-remainder.png" alt="Taylor級数・剰余・収束半径の図解" style="max-height: 480px; display:block; margin:0 auto;" />

図には $e^x$ と複数次数のTaylor多項式を重ねる。展開点 $a=0$ 付近ではどの多項式もよく一致するが、離れるほど低次数近似の誤差が大きい。次数を上げると一致する範囲が広がる様子が、剰余 $R_n(x)$ が小さくなることに対応する。

## 記号・型・定義域

| 記号 | 意味 |
|---|---|
| $P_n(x)$ | n次Taylor多項式 |
| $R_n(x)=f(x)-P_n(x)$ | 剰余 |
| $ξ$ | aとxの間の点 |


- $P_n(x)$：a周りn次Taylor多項式。
- $R_n(x)=f(x)-P_n(x)$：剰余。
- $\xi$：aとxの間に存在する点。

## 中心となる式

$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-a)^{n+1}
$$

## 中心式を前提から導く

1. n次まで導関数を一致させた差 $R_n$ を考える。
2. Rolleの定理を繰り返す補助関数から Lagrange 剰余形を得る。
3. 右辺が n→∞ で0へ行く範囲でTaylor級数がfへ一致する。

## なぜその変形をしてよいのか

Taylor多項式 $P_n$ は $a$ で0次からn次までの導関数を一致させる唯一の多項式だが、それだけでは $n\to\infty$ で $f$ に収束するとは限らない。必要なのは $R_n(x)=f(x)-P_n(x)\to0$。

Lagrange剰余 $R_n=f^{(n+1)}(\xi)(x-a)^{n+1}/(n+1)!$ は、微分可能性の仮定の下で誤差を「高階導関数の大きさ」と「距離の高べき」に分解する。$e^x$ では高階導関数も $e^x$ なので固定区間で上界を取り、階乗がべきより速く増えることから誤差0を示せる。

## Taylor多項式とTaylor級数を分離する

n次Taylor多項式

$$
P_n(x)=\sum_{k=0}^n\frac{f^{(k)}(a)}{k!}(x-a)^k
$$

は有限和なので、fがn回微分可能なら局所近似として扱える。一方Taylor級数

$$
\sum_{k=0}^{\infty}\frac{f^{(k)}(a)}{k!}(x-a)^k
$$

は無限級数であり、(1) 級数が収束するか、(2) その和がf(x)と一致するか、を別々に確認する必要がある。

## Lagrange剰余をどう使うか

$x$ とaを含む区間で

$$
|f^{(n+1)}(t)|\le M
$$

が分かれば

$$
|R_n(x)|\le \frac{M|x-a|^{n+1}}{(n+1)!}.
$$

右辺が0へ行けばTaylor級数はそのxでfへ一致する。たとえば $e^x$ では固定したxに対し区間上で $e^t\le e^{|x|}$ とでき、階乗がべきより速く成長するため全実数で一致する。

## 収束半径

一般の冪級数 $\sum c_n(x-a)^n$ には、ある $R\in[0,\infty]$ が存在して $|x-a|<R$ で絶対収束、$|x-a|>R$ で発散する。これが収束半径。端点 $|x-a|=R$ は個別判定が必要。

例として

$$
\sum_{n=0}^{\infty}x^n
$$

は $|x|<1$ で $1/(1-x)$ へ収束し、収束半径は1。Taylor級数が局所的な表現である理由を理解するうえで重要である。

## 例題1：具体的な数値・構造で解く

**問題**：$e^x$ を0周り2次Taylor多項式で $x=0.5$ に近似し、Lagrange剰余で誤差上界を求めよ。

**解答**：$P_2(0.5)=1+0.5+0.5^2/2=1.625$。$R_2=e^{\xi}0.5^3/3!$, $0<\xi<0.5$ なので $|R_2|\le e^{0.5}/48\approx0.03435$。

## 例題2：別の条件で確認する

$\sin x$ の0周り3次Taylor多項式は $x-x^3/6$。$|x|\le1$ なら次の非零項の大きさ $|x|^5/120\le1/120$ が誤差の目安になる。

## 結果の検算

近似値だけでなく剰余上界を同時に計算する。$|R_n(x)|\le M|x-a|^{n+1}/(n+1)!$ を使ったなら、選んだ区間上で本当に $|f^{(n+1)}|\le M$ か確認し、実際の誤差 $|f(x)-P_n(x)|$ が上界以下になることを数値で確かめる。

## 条件を外すと何が壊れるか

滑らかでもTaylor級数が元関数に一致しない例がある。$f(x)=e^{-1/x^2}$ ($x\ne0$), $f(0)=0$ は0で全導関数が0なのでTaylor級数は0だが、$x\ne0$ では $f(x)>0$。

## よくある誤り

- Taylor級数が形式的に書けることと関数へ収束することを同一視しない。
- 近似中心から遠い点では有限次数誤差を評価する。

## 次のTopic・応用への接続

数値計算ではTaylor展開からEuler法や有限差分の局所誤差を導く。最適化では二次近似、確率では特性関数や漸近展開に同じ考えが現れる。

## 参考

- MIT 18.01SC Taylor Series

[演習へ](/exercises/calc-taylor-series-remainder)　|　[スライドへ](/slides/calc-taylor-series-remainder/)
