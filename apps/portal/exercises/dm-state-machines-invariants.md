# 状態機械と不変量：演習

Course 04｜離散数学

[教科書](/textbook/dm-state-machines-invariants)

## 問題1

初期値 $x=1$、各stepで $x\leftarrow x+4$ または $x\leftarrow x-8$ とする。$x\equiv1\pmod4$ が不変量であることを示し、x=6が到達不能と結論せよ。

<details><summary>完全解答</summary>

初期1はmod4で1。+4も-8も4の倍数を加えるので剰余1を保存する。帰納法により全到達状態で $x\equiv1\pmod4$。6はmod4で2なので到達不能。

</details>

## 問題2

「状態機械と不変量」の導出を、最初の段階「1. 初期状態でIを確認する。」から始めて中心式まで再構成せよ。途中で「state machineを $(S,S_0,\to)$ として、状態集合S、初期状態集合 $S_0$、遷移関係 $\to$ を分ける。」がなぜ正当化できるかも説明すること。

<details><summary>完全解答</summary>

1. 初期状態でIを確認する。
2. 任意の1step遷移がIを保存することを示す。
3. 帰納法により任意step後の到達可能状態でIが成り立つ。

state machineを $(S,S_0,\to)$ として、状態集合S、初期状態集合 $S_0$、遷移関係 $\to$ を分ける。不変量Iは到達可能なすべての状態で真にしたい命題。証明は数学的帰納法そのもの。

baseとして全初期状態でIを示し、stepとして $I(s)$ かつ $s\to s\prime$ なら $I(s\prime)$ を示す。すると長さkの任意の実行について帰納的にIが保存される。個々の実行を列挙しなくてよいのが強み。

</details>

## 問題3

図 `/visuals/course-04/dm-state-machines-invariants.png` では「各円が状態、矢印が1stepで許された遷移。」と説明されている。図に実際に描かれた対象を少なくとも3つ挙げ、それぞれが数式中のどの量・演算・条件を表すか対応づけよ。

<details><summary>完全解答</summary>

<img src="/visuals/course-04/dm-state-machines-invariants.png" alt="状態機械と不変量の図解" style="max-height: 480px; display:block; margin:0 auto;" />

各円が状態、矢印が1stepで許された遷移。破線の囲みが性質 $I$ を満たす状態集合で、初期状態が囲みの中にあり、囲みの中から出る遷移が存在しないなら、何step進めても外へ出ない。この閉性が不変量の帰納stepに対応する。

</details>

## 問題4

「状態機械と不変量」の第二例「整数xを0から開始し、各stepでxへ2を足す機械を考える。」を途中式つきで再現し、例題1と比べて変更された条件が結論へどう効いたか説明せよ。

<details><summary>完全解答</summary>

整数xを0から開始し、各stepでxへ2を足す機械を考える。「xは偶数」が不変量。初期0は偶数、偶数+2は偶数なので全到達状態で偶数。したがってx=5は到達不能。

</details>

## 問題5

状態機械と不変量で 状態集合、遷移関係、状態sで成り立たせたい不変量 は互いに何が違う量か。各量の数学的な種類（scalar/vector/matrix/set/function/distribution等）と、必要な値域・shape・制約を記号表に沿って整理せよ。 最後に、`dm-state-machines-invariants` の中心式の中でこれらの量がどこに現れるかを1箇所示せ。

<details><summary>完全解答</summary>

| 記号 | 意味 |
|---|---|
| $S$ | 状態集合 |
| $→$ | 遷移関係 |
| $I(s)$ | 状態sで成り立たせたい不変量 |


- $S$：状態集合、$s_0$：初期状態。
- $s\to s'$：1step遷移。
- $I(s)$：状態sで真偽を持つ不変量候補。

</details>

## 問題6

警告「初期状態で真でもstep保存を示していなければ不変量ではない。」が必要な理由を、中心式のどの段階が壊れるかまで示して説明せよ。可能なら最小の数値例・反例を1つ添えよ。

<details><summary>完全解答</summary>

初期状態で真でもstep保存を示していなければ不変量ではない。逆に保存されても初期状態で偽なら到達状態について何も言えない。baseとstepを必ず分離する。

</details>

## 問題7

よくある誤り「観察した数stepだけから不変と断定しない。」を犯した答案を想定し、どの一行が誤りかを特定して正しい計算・論証へ修正せよ。

<details><summary>完全解答</summary>

- 観察した数stepだけから不変と断定しない。
- 安全性不変量と停止性は別の証明。

初期状態で真でもstep保存を示していなければ不変量ではない。逆に保存されても初期状態で偽なら到達状態について何も言えない。baseとstepを必ず分離する。

</details>

## 問題8

「状態機械と不変量」の例題1を再計算し、その結果に対して次の検算を実行せよ：不変量候補は、初期状態だけでなく**許される全種類の遷移**について保存性を確認する。 単に「一致した」と書かず、検算用の式・数値・条件を明示すること。

<details><summary>完全解答</summary>

初期1はmod4で1。+4も-8も4の倍数を加えるので剰余1を保存する。帰納法により全到達状態で $x\equiv1\pmod4$。6はmod4で2なので到達不能。

検算：
不変量候補は、初期状態だけでなく**許される全種類の遷移**について保存性を確認する。到達不能と結論したstateについては、不変量の値が初期stateと本当に異なるかを計算する。数stepのsimulationだけを証明の代わりにしない。

</details>

## 問題9

後続への接続「loop invariant、distributed protocol、安全性証明へ直結する。」を具体化せよ。このTopicで得る量を1つ選び、それが次のTopicでどの入力になり、どの演算を受け、何を出力するかを式またはalgorithm名とともに説明せよ。

<details><summary>完全解答</summary>

loop invariant、distributed protocol、安全性証明へ直結する。動的系や最適化アルゴリズムでも「反復で保たれる集合・量」を探す考えが再登場する。

</details>

## 問題10

中心問題「時間とともに状態が変わる系の正しさを、毎step追わずにどう証明するか。」に対し、試験答案として一続きに答えよ。必ず (1) 主要記号の定義、(2) 中心式 `$$ I(s_0)\land[I(s)\land s\to s\prime\Rightarrow I(s\prime)] $$`, (3) 導出の根拠、(4) 例題1の具体値、(5) 「初期状態で真でもstep保存を示していなければ不変量ではない。」という失敗条件、の5点を含めること。

<details><summary>完全解答</summary>

主要記号：
| 記号 | 意味 |
|---|---|
| $S$ | 状態集合 |
| $→$ | 遷移関係 |
| $I(s)$ | 状態sで成り立たせたい不変量 |


- $S$：状態集合、$s_0$：初期状態。
- $s\to s'$：1step遷移。
- $I(s)$：状態sで真偽を持つ不変量候補。

中心式：
$$
I(s_0)\land[I(s)\land s\to s\prime\Rightarrow I(s\prime)]
$$

導出：
1. 初期状態でIを確認する。
2. 任意の1step遷移がIを保存することを示す。
3. 帰納法により任意step後の到達可能状態でIが成り立つ。

根拠：
state machineを $(S,S_0,\to)$ として、状態集合S、初期状態集合 $S_0$、遷移関係 $\to$ を分ける。不変量Iは到達可能なすべての状態で真にしたい命題。証明は数学的帰納法そのもの。

baseとして全初期状態でIを示し、stepとして $I(s)$ かつ $s\to s\prime$ なら $I(s\prime)$ を示す。すると長さkの任意の実行について帰納的にIが保存される。個々の実行を列挙しなくてよいのが強み。

具体例：
**問題**：初期値 $x=1$、各stepで $x\leftarrow x+4$ または $x\leftarrow x-8$ とする。$x\equiv1\pmod4$ が不変量であることを示し、x=6が到達不能と結論せよ。

**解答**：初期1はmod4で1。+4も-8も4の倍数を加えるので剰余1を保存する。帰納法により全到達状態で $x\equiv1\pmod4$。6はmod4で2なので到達不能。

失敗条件：
初期状態で真でもstep保存を示していなければ不変量ではない。逆に保存されても初期状態で偽なら到達状態について何も言えない。baseとstepを必ず分離する。

</details>
