# 尤度と最尤推定：演習

Course 03｜Topic 15/20。10問すべて、このTopic固有の問いとして作成しています。

[教科書](/textbook/stat-likelihood-maximum-likelihood)

## 問1. 定義と記号

「尤度と最尤推定」の代表式

$$
\hat{\theta}_{\mathrm{MLE}}=\arg\max_{\theta}L(\theta)
$$

について、左辺が表す量、右辺の各主要量、式を使う目的を文章で説明せよ。未定義の記号を残さないこと。

<details><summary>ヒント</summary>

式を日本語へ翻訳し、入力・出力・parameter・条件を分ける。

</details>

<details><summary>完全解答</summary>

代表式は結果だけを書くための記号ではない。本文で定義した量を使い、尤度と最尤推定が何を計算・比較・最適化しているかを説明する。特に次の最初の導出が式の役割を具体化する。

**同時密度を母数の関数として読む**

データを観測後は $x_i$ を固定し、$p(\mathbf x\mid\theta)$ をθについて比較する。これがlikelihood。

答案では式の両辺の型・次元または確率的役割まで整合していることを確認する。

</details>

## 問2. 導出1：同時密度を母数の関数として読む

「尤度と最尤推定」で **同時密度を母数の関数として読む** が必要になる理由を述べ、本文の途中式・論理を自分で再現せよ。結果だけでなく、どの定義または仮定を使ったかを書くこと。

<details><summary>ヒント</summary>

本文の「同時密度を母数の関数として読む」を、直前の定義から始めて書き直す。

</details>

<details><summary>完全解答</summary>

この段階で示すべき内容は次である。

データを観測後は $x_i$ を固定し、$p(\mathbf x\mid\theta)$ をθについて比較する。これがlikelihood。

重要なのはこの結果を独立な公式として置かず、直前までの定義・仮定から導くことである。次の「独立なら積、logで和」へ進むときも、この段階で得た量だけを使う。

</details>

## 問3. 導出2：独立なら積、logで和

「尤度と最尤推定」で **独立なら積、logで和** が必要になる理由を述べ、本文の途中式・論理を自分で再現せよ。結果だけでなく、どの定義または仮定を使ったかを書くこと。

<details><summary>ヒント</summary>

本文の「独立なら積、logで和」を、直前の定義から始めて書き直す。

</details>

<details><summary>完全解答</summary>

この段階で示すべき内容は次である。

$L(\theta)=\prod_i p(x_i\mid\theta)$。logは単調増加なのでargmaxは変わらず、$\ell(\theta)=\sum_i\log p(x_i\mid\theta)$。

重要なのはこの結果を独立な公式として置かず、直前までの定義・仮定から導くことである。次の「微分して最適条件を解く」へ進むときも、この段階で得た量だけを使う。

</details>

## 問4. 導出3：微分して最適条件を解く

「尤度と最尤推定」で **微分して最適条件を解く** が必要になる理由を述べ、本文の途中式・論理を自分で再現せよ。結果だけでなく、どの定義または仮定を使ったかを書くこと。

<details><summary>ヒント</summary>

本文の「微分して最適条件を解く」を、直前の定義から始めて書き直す。

</details>

<details><summary>完全解答</summary>

この段階で示すべき内容は次である。

内部の滑らかな最大点なら $d\ell/d\theta=0$。Bernoulliならこの方程式から $\hat p=\bar x$ が出る。二階微分や境界も確認して最大であることを確かめる。

重要なのはこの結果を独立な公式として置かず、直前までの定義・仮定から導くことである。次の「最終結論」へ進むときも、この段階で得た量だけを使う。

</details>

## 問5. 数値例を途中から再現

次の「尤度と最尤推定」の設定を、自分で途中量まで展開して最終結論を確認せよ。

> Bernoulli観測で成功k回、失敗n-k回。$\ell(p)=k\log p+(n-k)\log(1-p)$。微分して $k/p-(n-k)/(1-p)=0$、整理すると $\hat p=k/n$。

本文の結論を引用するだけでなく、少なくとも1つ中間計算・中間判断を示すこと。

<details><summary>完全解答</summary>

設定に対する計算・判断は次の通り。

Bernoulli観測で成功k回、失敗n-k回。$\ell(p)=k\log p+(n-k)\log(1-p)$。微分して $k/p-(n-k)/(1-p)=0$、整理すると $\hat p=k/n$。

ここで得た値だけでなく、代表式のどの量へ代入したか、また結果の符号・確率範囲・shape・単位などが妥当かを検算する。

</details>

## 問6. 条件を変えたときの差

次の第二例について、第一例から変更した条件を特定し、その変更によって「尤度と最尤推定」のどの部分が変わるか説明せよ。

> 正規分布で分散既知なら $\ell(\mu)=const-\sum_i(x_i-\mu)^2/(2\sigma^2)$。最大化は二乗誤差最小化と同値で、$\hat\mu=\bar x$。

<details><summary>完全解答</summary>

正規分布で分散既知なら $\ell(\mu)=const-\sum_i(x_i-\mu)^2/(2\sigma^2)$。最大化は二乗誤差最小化と同値で、$\hat\mu=\bar x$。

比較では、定義そのものが変わったのか、parameterだけが変わったのか、成立条件が変わったのかを区別する。同じ代表式が使える場合は、なぜ使える条件が保たれているかも述べる。

</details>

## 問7. 成立条件と反例

「尤度と最尤推定」について、本文の成立条件を確認したうえで、次の失敗例で何が壊れているか診断せよ。

> 尤度 $L(\theta)$ はθの確率分布ではないため、θについて積分して1になる必要はない。「尤度0.8だからθの確率80%」とは読めない。

<details><summary>ヒント</summary>

「式が未定義」「解が非一意」「近似が悪い」「確率解釈が崩れる」など失敗の種類を分ける。

</details>

<details><summary>完全解答</summary>

本文で確認する条件は以下である。

- 尤度をθについて積分して1にする必要はない。
- 対数尤度は最大点を変えない。
- 尤度と最尤推定の定義と計算手順を区別し、数値例だけで一般性を判断しない。

失敗例は次の通り。

尤度 $L(\theta)$ はθの確率分布ではないため、θについて積分して1になる必要はない。「尤度0.8だからθの確率80%」とは読めない。

したがって、どの仮定を外したため、代表式またはその解釈のどの部分まで保証できなくなったかを対応づけて説明する。

</details>

## 問8. 実装・数値診断

「尤度と最尤推定」を実装するときの次の注意点について、数学的に正しい式とcomputer上の計算がなぜ同じ安全性を持たないか説明せよ。

> 積の尤度はunderflowするためlog-likelihoodを使う。最適化ではgradientだけでなくbound・constraint・複数極値も確認する。

<details><summary>完全解答</summary>

積の尤度はunderflowするためlog-likelihoodを使う。最適化ではgradientだけでなくbound・constraint・複数極値も確認する。

実装答案では、単にlibrary関数名を書くのではなく、overflow/underflow、conditioning、data leakage、finite precision、停止条件など、このTopicで問題になる原因と対策を結び付ける。

</details>

## 問9. 次Topicへの導線

「尤度と最尤推定」から次の発展へ進む論理を、未学習概念を途中で仮定せず説明せよ。

> MLEに事前分布を掛けるとposteriorが得られ、posteriorのmodeを取ればMAP推定。次TopicでBayesian推論として区別する。

<details><summary>完全解答</summary>

MLEに事前分布を掛けるとposteriorが得られ、posteriorのmodeを取ればMAP推定。次TopicでBayesian推論として区別する。

本文で既に得た定義・式のうち何を一般化または再利用するかを明示する。後続Topicで初めて定義する対象が必要なら、ここでは必要性の説明までに留める。

</details>

## 問10. 総合証明・説明

「尤度と最尤推定」を、(1)前提、(2)代表式、(3)導出の3段階、(4)数値例、(5)反例、(6)実装上の注意、の順で説明せよ。各段階の因果関係が分かる答案にすること。

<details><summary>完全解答</summary>

答案では次の流れを一続きにする。

**代表式**

$$
\hat{\theta}_{\mathrm{MLE}}=\arg\max_{\theta}L(\theta)
$$

**導出**

1. **同時密度を母数の関数として読む** — データを観測後は $x_i$ を固定し、$p(\mathbf x\mid\theta)$ をθについて比較する。これがlikelihood。

2. **独立なら積、logで和** — $L(\theta)=\prod_i p(x_i\mid\theta)$。logは単調増加なのでargmaxは変わらず、$\ell(\theta)=\sum_i\log p(x_i\mid\theta)$。

3. **微分して最適条件を解く** — 内部の滑らかな最大点なら $d\ell/d\theta=0$。Bernoulliならこの方程式から $\hat p=\bar x$ が出る。二階微分や境界も確認して最大であることを確かめる。

**数値・具体例**

Bernoulli観測で成功k回、失敗n-k回。$\ell(p)=k\log p+(n-k)\log(1-p)$。微分して $k/p-(n-k)/(1-p)=0$、整理すると $\hat p=k/n$。

**条件を壊すと**

尤度 $L(\theta)$ はθの確率分布ではないため、θについて積分して1になる必要はない。「尤度0.8だからθの確率80%」とは読めない。

**実装**

積の尤度はunderflowするためlog-likelihoodを使う。最適化ではgradientだけでなくbound・constraint・複数極値も確認する。

各節を独立な箇条書きにせず、「前の結果が次の式をなぜ許すか」を接続して書く。

</details>
