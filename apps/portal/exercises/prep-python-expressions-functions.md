# Pythonの式・変数・関数：演習

[教科書](/textbook/prep-python-expressions-functions)と対応する10問。各問題は独立した学習目標を持ち、旧版のような同一文面の反復を避けている。

## PREP-PY-01：概念確認

`x = 2; x = x + 3` の最終的な `x` と、数学の等式 `x=x+3` との違いを説明せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

最終値は5。Pythonの`=`は代入で、2に3を足した値を名前xへ再束縛する。数学の等号とは意味が異なる。

:::

::: details よくある誤答

方程式なので解なしと答える。

:::

## PREP-PY-02：概念確認

Pythonで $2^5$ を計算する正しい式を書け。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`2 ** 5` で32。`2 ^ 5` はbitwise XORで7。

:::

::: details よくある誤答

`2 ^ 5` を使う。

:::

## PREP-PY-03：手計算・導出

`7 / 2` と `7 // 2` の違いを答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`7 / 2` は3.5、`7 // 2` はfloor divisionで3。

:::

::: details よくある誤答

どちらも整数3とする。

:::

## PREP-PY-04：手計算・導出

数学の $i=1,2,3,4$ を回す `range` を書け。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`range(1,5)`。stopは含まれない。

:::

::: details よくある誤答

`range(1,4)` とする。

:::

## PREP-PY-05：手計算・導出

`def affine(x,a,b): return a*x+b` で `affine(3,2,1)` を計算し、各引数を数学式に対応付けよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

$a=2,x=3,b=1$ なので7。関数は $f(x)=ax+b$ を計算する。

:::

::: details よくある誤答

引数順序を $x,b,a$ と混同する。

:::

## PREP-PY-06：誤りの診断

平方和を返す関数を `for` を使って書き、`[1,2,3]` で検算せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

最小の反例または定義との不一致を一つ示す。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`total=0.0` から各valueについて `total += value**2`、最後にreturn。結果は14。

:::

::: details よくある誤答

`total = value**2` と毎回上書きする。

:::

## PREP-PY-07：誤りの診断

`if x > 0: ... elif x == 0: ... else: ...` が実数をどの3場合へ分けるか説明せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

最小の反例または定義との不一致を一つ示す。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

正、0、負の互いに排他的で全実数を覆う3場合。

:::

::: details よくある誤答

`else`が0も含むと考える。

:::

## PREP-PY-08：実装・数値読解

型注釈 `x: float` がある関数へ整数 `3` を渡すと必ず実行時エラーになるか。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

標準Pythonの型注釈だけでは通常強制されない。整数を受け取って処理が進む場合もある。

:::

::: details よくある誤答

型注釈がruntime validatorだと考える。

:::

## PREP-PY-09：応用

グローバル変数 `scale` に依存する関数と、`scale` を引数で受け取る関数を再現性の観点から比較せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

引数で受け取る方が依存関係が明示され、同じ入力で挙動を再現・テストしやすい。

:::

::: details よくある誤答

グローバル変数の方が短いので常に優れているとする。

:::

## PREP-PY-10：応用

数値関数をデバッグするとき「最小の既知ケース」を使う理由を説明せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

答えを出す前に、小さな展開・定義・手計算のどれかで根拠を確認する。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

手計算で期待値を独立に求められ、中間値と比較して誤り箇所を限定しやすいから。

:::

::: details よくある誤答

大規模データの方が実運用に近いので最初から使うべきとする。

:::
