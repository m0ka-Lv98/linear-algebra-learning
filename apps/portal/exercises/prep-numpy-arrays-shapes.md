# NumPy配列・shape・indexing：演習

[教科書](/textbook/prep-numpy-arrays-shapes)と対応する10問。各問題は独立した学習目標を持ち、旧版のような同一文面の反復を避けている。

## PREP-NP-01：概念確認

`A=np.zeros((2,3,4))` の `ndim`、`shape`、`size` を答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`ndim=3`、`shape=(2,3,4)`、`size=24`。

:::

::: details よくある誤答

数学の次元24を`ndim`と答える。

:::

## PREP-NP-02：概念確認

`A=np.array([[1,2,3],[4,5,6]])` の `A[0,2]`、`A[:,1]`、`A[1,:]` を答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`A[0,2]=3`、`A[:,1]=[2,5]`、`A[1,:]=[4,5,6]`。

:::

::: details よくある誤答

1始まりindexで読む。

:::

## PREP-NP-03：手計算・導出

上の `A[:,1]` と `A[:,1:2]` のshapeを比較せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

前者は `(2,)`、後者は `(2,1)`。sliceで範囲を残すと軸が維持される。

:::

::: details よくある誤答

値が同じなのでshapeも同じとする。

:::

## PREP-NP-04：手計算・導出

`x=np.array([1,2,3])` の `x.T.shape` と `x.reshape(-1,1).shape` を答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`x.T.shape==(3,)`、reshape後は `(3,1)`。

:::

::: details よくある誤答

`.T`だけで `(1,3)` になるとする。

:::

## PREP-NP-05：手計算・導出

shape `(2,6)` の配列を `(3,4)` にreshapeできるか。理由も答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

総要素数が12で一致するので可能。ただし軸の意味が適切かは別問題。

:::

::: details よくある誤答

元shapeの各軸が割り切れないので不可とする。

:::

## PREP-NP-06：誤りの診断

`A.shape=(2,3)` に対して `np.sum(A,axis=0)` と `axis=1` の結果shapeを答えよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

最小の反例または定義との不一致を一つ示す。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

axis=0では第0軸が消え `(3,)`、axis=1では第1軸が消え `(2,)`。

:::

::: details よくある誤答

axis番号を残る軸番号と考える。

:::

## PREP-NP-07：誤りの診断

`A.shape=(2,3)`、`B.shape=(3,4)` のとき `A @ B` のshapeを答え、`A * B` がそのまま可能か判断せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

最小の反例または定義との不一致を一つ示す。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

`A @ B` は `(2,4)`。`A * B` はshape `(2,3)` と `(3,4)` が通常broadcast互換でないためエラー。

:::

::: details よくある誤答

`*`も同じ行列積になるとする。

:::

## PREP-NP-08：実装・数値読解

`A=np.arange(6).reshape(2,3); B=A[:,1:]` とした。`B[0,0]=99` が `A` に影響する可能性を説明せよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

basic slicingで得た`B`はviewであることが多く、元`A`とメモリ共有していれば対応要素も99へ変わる。独立化には`B=A[:,1:].copy()`。

:::

::: details よくある誤答

sliceは必ずcopyだとする。

:::

## PREP-NP-09：応用

画像配列shape `(32,224,224,3)` の各軸の意味の一例を説明し、`reshape(32,-1)` が何を失う可能性があるか述べよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

コードを実行したつもりで、各行の入力・出力・shapeまたは値を順に追う。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

batch, height, width, channelの例。flattenすると空間軸・channel軸の構造的意味を一つのfeature軸へ潰す。値は保持してもsemantic structureが変わる。

:::

::: details よくある誤答

要素数が同じなので意味も完全に同じとする。

:::

## PREP-NP-10：応用

数学の行列 $\mathbf{A}\in\mathbb{R}^{m\times n}$ とNumPy配列を対応させるとき、最低限記録すべき3点を挙げよ。

::: details ヒント1

まず対象・記号・shape・定義域のうち、この問題で必要なものを書き出す。

:::

::: details ヒント2

答えを出す前に、小さな展開・定義・手計算のどれかで根拠を確認する。

:::

::: details 解法方針

定義と成立条件を先に確認し、必要なら式を展開する。数値・コード問題では期待する型または値を先に予測してから計算する。

:::

::: details 完全解答

数学上の対象・次元、NumPyのshape/dtype、各軸が何を意味するか（例：sample/feature）を記録する。

:::

::: details よくある誤答

配列の値だけ保存すれば十分とする。

:::
