# アーキテクチャ

VitePressを学習ポータルの入口にし、教科書・演習・テーマ導線を静的サイトとしてまとめます。Slidevは概要と直感に集中したスライドを担当します。

教科書とスライドは目的・粒度・読書経路が異なるため本文を共通化しません。一方、将来の可視化はVueコンポーネントだけを `packages/visualizations` に置き、両方から共有できる方針です。

Python/Jupyterは教材基盤が安定した後、独立した実行環境として追加します。
